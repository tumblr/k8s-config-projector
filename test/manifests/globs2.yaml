# Config repo for tumblr app
name: globs2
namespace: test
data:
# all three items are appropriate for specifying globbing raw files
# note the source format=glob always
# note the output_format=raw always
# note the output_file is always missing
- source: "doods/*.php"
  source_format: glob
